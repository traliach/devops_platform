# Populated from: terraform -chdir=infra output instance_id
# Connection uses AWS SSM — no SSH port or key pair required
# Update the instance ID here after every terraform apply (if instance is recreated)

[platform]
i-0eb277f732ee785ac

[platform:vars]
ansible_connection              = community.aws.aws_ssm
ansible_aws_ssm_bucket_name     = achille-tf-state
ansible_aws_ssm_region          = us-east-1
ansible_python_interpreter      = /usr/bin/python3
